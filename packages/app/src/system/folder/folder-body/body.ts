const backBtnIsActive = ref<boolean>(true); // 后退
const frontBtnIsActive = ref<boolean>(true); // 前进
const historyBtnIsActive = ref<boolean>(true); // 历史
const moveupBtnIsActive = ref<boolean>(true); // 上移

export { backBtnIsActive, frontBtnIsActive, historyBtnIsActive, moveupBtnIsActive };
