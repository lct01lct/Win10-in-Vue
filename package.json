{
  "homepage": "https://lct01lct.github.io/",
  "scripts": {
    "dev": "pnpm -C packages/render-core dev",
    "dev:play": "pnpm -C packages/app-store dev",
    "build": "pnpm -C packages/render-core build",
    "build:app-core": "pnpm -C packages/render-core build:app-core",
    "build:app-store": "pnpm -C packages/app-store build:app-store",
    "preview": "pnpm -C packages/render-core preview",
    "commit": "git add . && git cz"
  },
  "dependencies": {
    "cz-conventional-changelog": "^3.3.0"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  },
  "devDependencies": {
    "gh-pages": "^5.0.0"
  }
}