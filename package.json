{
  "scripts": {
    "dev": "pnpm -F win10 dev --open",
    "build": "pnpm -F win10 build",
    "preview": "pnpm -F win10 preview",
    "commit": "git cz"
  },
  "dependencies": {
    "cz-conventional-changelog": "^2.1.0"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}